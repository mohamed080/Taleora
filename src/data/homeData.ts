export type ValueItem = {
  title: string;
  text: string;
  icon: string;
};

export type BookItem = {
  title: string;
  author: string;
  level: string;
  color: string;
};

export const navLinks = ["home", "books", "authors", "community", "about"];

